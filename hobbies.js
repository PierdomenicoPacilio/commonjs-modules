const hobbiesArray = (hobbyOne, hobbyTwo, hobbyThree) => { return [`${hobbyOne}`, `${hobbyTwo}`, `${hobbyThree}`]; };

console.log(hobbiesArray('calcio', 'giardinaggio', 'lettura'));

module.exports = hobbiesArray;